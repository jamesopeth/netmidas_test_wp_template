CREATE TABLE wp_contacts (
         id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
         name VARCHAR(100),
         email VARCHAR(100),
         subject VARCHAR(100),
         message VARCHAR(1000)
       );
